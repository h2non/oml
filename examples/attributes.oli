div@id.left.container(class: 'user'):
  h1.title: name
  form:
    input(type: 'text', name: 'user[name]', value: name)
    input(checked, type: 'checkbox', name: 'user[blocked]')
    input(type: 'submit', value: 'Update')
  end
end